import DashboardLayout from "../dashboard/layout";
import { Typography } from "@mui/material";
export default function VendasPage() { return <DashboardLayout><Typography variant="h4">Vendas</Typography><Typography sx={{ mt: 1 }}>O registro de vendas ainda não está disponível neste projeto.</Typography></DashboardLayout>; }
