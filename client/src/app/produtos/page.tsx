import DashboardLayout from "../dashboard/layout";
import { Typography } from "@mui/material";
export default function ProdutosPage() { return <DashboardLayout><Typography variant="h4">Produtos</Typography><Typography sx={{ mt: 1 }}>O cadastro de produtos ainda não está disponível neste projeto.</Typography></DashboardLayout>; }
