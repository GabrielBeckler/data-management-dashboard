import DashboardLayout from "../dashboard/layout";
import { Typography } from "@mui/material";
export default function EstoquePage() { return <DashboardLayout><Typography variant="h4">Estoque</Typography><Typography sx={{ mt: 1 }}>O controle de estoque ainda não está disponível neste projeto.</Typography></DashboardLayout>; }
