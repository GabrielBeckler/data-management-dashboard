import DashboardLayout from "../dashboard/layout";
import { Typography } from "@mui/material";
export default function ConsultasPage() { return <DashboardLayout><Typography variant="h4">Consultas</Typography><Typography sx={{ mt: 1 }}>A agenda de consultas continuará vinculada ao fluxo existente de agendamentos.</Typography></DashboardLayout>; }
