import DashboardLayout from "../dashboard/layout";
import { Typography } from "@mui/material";
export default function ClientesPage() { return <DashboardLayout><Typography variant="h4">Clientes</Typography><Typography sx={{ mt: 1 }}>A gestão de clientes ainda não possui uma API administrativa.</Typography></DashboardLayout>; }
